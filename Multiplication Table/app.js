var numbe = +prompt("By which Number")
var start = +prompt("Enter the Starting value")
var endee = +prompt("Enter the Ending value")

for(var i = start; i <= endee; i++){
        document.write(`${numbe} x ${i} = ${start*i} <br>`)
    }